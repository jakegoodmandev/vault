"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "pnpm install"
run = "pnpm run dev:web --port $CONDUCTOR_PORT"

# Using "nonconcurrent" because the run script depends on the single local supabase port (54322)
run_mode = "nonconcurrent"
