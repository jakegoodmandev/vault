"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "pnpm install"
run = "pnpm run dev:web --port $CONDUCTOR_PORT"

# web can use per-workspace ports; Supabase is shared
run_mode = "concurrent"
